<div id="<%= dom_id(message) %>" class="msg msg-<%= message.user.id %> <%= defined?(pagination) && 'paginaton' || 'not-pagination'%>">
  <h6 class="name"> <%= message.user.name %> </h6>
  <h5><%= message.body %></h5>
</div>
