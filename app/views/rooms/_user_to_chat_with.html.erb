<%= form_with( model: @room, remote: true, class: 'd-flex usermail') do |f|%>
  <%= user.name %>
  <%= hidden_field_tag :receiver_user_id, user.id  %>
  <%= f.submit 'conversar', data: {disable_with: false }%>
<% end %>
