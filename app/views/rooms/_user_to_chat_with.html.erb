<%= form_with( model: @room, remote: true, class: 'd-flex') do |f|%>
  <%= user.email %>
  <%= hidden_field_tag 'user', user.email  %>
  <%= f.submit 'conversar', data: {disable_with: false } %>
<% end %>

